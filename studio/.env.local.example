# Base URL for the lighting-engine FastAPI service.
# Set NEXT_PUBLIC_API_URL to the engine's URL when deployed.
# Defaults to http://localhost:8000 if unset.
NEXT_PUBLIC_API_URL=http://localhost:8000
